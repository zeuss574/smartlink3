<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>create music smartlink</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Mulish:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <script src="https://www.google.com/recaptcha/api.js?render=<%= siteKey %>"></script>
</head>
<body class="home-page">
    <div class="container">
        <h1>music smartlink creation</h1>
        <p class="subtitle">create a simple music smartlink to share it everywhere</p>

        <% if (error) { %>
            <div class="message error"><%= error %></div>
        <% } %>
        <% if (success) { %>
            <div class="message success">
                <a href="<%= success.split(' ').pop() %>" target="_blank"><%= success %></a>
            </div>
        <% } %>

        <form action="/create" method="POST">
            <input type="hidden" name="g-recaptcha-response" id="g-recaptcha-response">
            <input type="url" name="musicUrl" placeholder="paste music URL (Spotify, Apple Music,...)" required>
            <input type="text" name="title" placeholder="create a custom URL path" required pattern="[a-zA-Z0-9-_]+">
            <button type="submit">create</button>
        </form>
    </div>
    <script>
      document.querySelector('form').addEventListener('submit', function(event) {
        event.preventDefault();
        grecaptcha.ready(function() {
          grecaptcha.execute('<%= siteKey %>', {action: 'submit'}).then(function(token) {
            document.getElementById('g-recaptcha-response').value = token;
            document.querySelector('form').submit();
          });
        });
      });
    </script>
</body>
</html>
